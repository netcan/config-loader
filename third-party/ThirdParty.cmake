if (${BUILD_TESTS})
    add_subdirectory(third-party/Catch2)
endif()

if (HAS_TINYXML2)
    target_compile_definitions(${PROJECT_NAME} PUBLIC HAS_TINYXML2)
    add_subdirectory(third-party/tinyxml2)
    list(APPEND THIRD_PARTY_LIBRARIES tinyxml2::tinyxml2)
endif()

if (HAS_JSONCPP)
    target_compile_definitions(${PROJECT_NAME} PUBLIC HAS_JSONCPP)
    add_subdirectory(third-party/jsoncpp)
    list(APPEND THIRD_PARTY_LIBRARIES jsoncpp_static)
endif()

if (HAS_YAMLCPP)
    target_compile_definitions(${PROJECT_NAME} PUBLIC HAS_YAMLCPP)
    add_subdirectory(third-party/yaml-cpp)
    list(APPEND THIRD_PARTY_LIBRARIES yaml-cpp)
endif()